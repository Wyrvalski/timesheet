spring:
  application:
    name: timesheet-eureka
  cloud:
    config:
      uri: http://localhost:9090
      fail-fast: true
      username: user
      password: 1234

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
